{
  "name": "health-hub-research-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node scripts/build.cjs",
    "setup": "node scripts/setup.cjs",
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --test tests/*.test.cjs"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
